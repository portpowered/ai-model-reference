{
  "title": "Flow matching",
  "description": "A generative training objective that teaches a model how samples should move from a simple or noisy starting state toward real data.",
  "openingSummary": "Flow matching is a way to train a generative model to predict how samples should move from a simple or noisy starting distribution toward real data, so generation can follow learned update steps instead of relying only on classic denoising wording.",
  "sections": {
    "whatItIs": {
      "title": "What It Is",
      "body": "Flow matching is a generative training objective, not a single model architecture. Training pairs a current sample state—often a noisy or easy-to-draw starting point—with a target direction that points toward real data. The model learns to predict that direction at many intermediate states along the path from noise to data. At generation time, the system starts from a simple draw, applies the learned update rule repeatedly, and walks the sample toward a finished image, audio clip, or other output."
    },
    "whyItMatters": {
      "title": "Why It Matters",
      "body": "Modern image and video systems need a stable way to teach iterative generation without forcing every team to use the same denoising vocabulary. Flow matching gives a plain-language framing for velocity or transport-style training: the model learns how states should change, not only how to subtract noise at one fixed schedule. That makes it easier to compare papers and product pages that talk about rectified flow, velocity prediction, or flow-style samplers against older diffusion descriptions."
    },
    "simpleExample": {
      "title": "Simple Example",
      "body": "Imagine training on images by starting each example as random noise and asking the network, at many points along the path back to the real photo, which small move would bring the current noisy canvas closer to the target. After training, generation begins from fresh noise and repeats those learned moves for a fixed number of steps. The finished sample is whatever state you reach when the schedule ends—similar in spirit to iterative diffusion sampling, but described as following a learned flow rather than only removing noise."
    },
    "vectorFieldIntuition": {
      "title": "Vector Field Intuition",
      "body": "A vector field is a learned direction-and-speed rule: at each current sample state, it answers which way to move next and how strongly. Picture many small arrows placed over possible states. Flow matching trains a network to output the arrow that matches the state it sees now.\n\nTraining builds pairs from a simple starting draw and a real data example. A time or noise level picks where you are along the transport path between them, producing a current state such as an interpolated blend. The training target is the velocity—the direction and speed that would move that current state toward data along the chosen path. The loss compares the model's predicted velocity to that target across many examples, starting draws, and times.\n\nAt generation time, the system draws a fresh simple start and repeatedly asks the model for the next small move, following the learned field step by step until the schedule ends. The diagram below shows that training loop, and the equation summarizes the velocity-matching objective in compact notation."
    },
    "commonConfusions": {
      "title": "Common Confusions",
      "body": "Flow matching is not the name of one product model or benchmark leaderboard. It is an objective family used inside many image and video stacks. It is also not identical to every diffusion system: some diffusion trainers still describe the network as predicting noise or a denoised sample, while flow matching usually describes predicting a velocity or transport direction. The two families can share similar sampling loops, but the training wording and target quantity are not always interchangeable."
    },
    "related": {
      "title": "Related Concepts And Modules"
    },
    "tags": {
      "title": "Tags"
    },
    "references": {
      "title": "References"
    }
  },
  "assets": {
    "vectorFieldFlow": {
      "title": "Flow matching vector field training flow",
      "alt": "A four-step flow matching training flow: pair a simple start with data, build a current state at time t, predict velocity, then follow the learned flow at generation.",
      "caption": "Training pairs current states and times with target velocities toward data; generation starts from a fresh draw and follows the learned field.",
      "legend": {
        "data-flow": {
          "label": "Training and sampling flow"
        }
      }
    }
  },
  "graph": {
    "nodes": {
      "dataPair": {
        "label": "Simple start x₀ and data example x₁"
      },
      "currentState": {
        "label": "Current state xₜ at time t"
      },
      "velocityPrediction": {
        "label": "Model predicts velocity vθ(xₜ, t)"
      },
      "flowSampling": {
        "label": "Generation follows the learned flow"
      }
    }
  },
  "math": {
    "velocityMatchingObjective": {
      "label": "Velocity matching objective",
      "formula": "\\mathcal{L} = \\mathbb{E}_{t,\\,x_0,\\,x_1}\\left[\\left\\lVert v_\\theta(x_t, t) - (x_1 - x_0) \\right\\rVert^2\\right]",
      "variableDefinitions": {
        "theta": {
          "term": "\\theta",
          "definition": "model weights"
        },
        "x0": {
          "term": "x_0",
          "definition": "a simple starting draw such as noise"
        },
        "x1": {
          "term": "x_1",
          "definition": "a real data example"
        },
        "t": {
          "term": "t",
          "definition": "transport time along the path between start and data"
        },
        "xt": {
          "term": "x_t",
          "definition": "the current interpolated state at time t"
        },
        "vtheta": {
          "term": "v_\\theta",
          "definition": "the velocity the model predicts at state x_t and time t"
        },
        "targetvelocity": {
          "term": "x_1 - x_0",
          "definition": "the target velocity along the straight transport path"
        }
      }
    }
  }
}
