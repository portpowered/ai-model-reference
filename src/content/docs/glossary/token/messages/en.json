{
  "title": "Token",
  "description": "The smallest unit of text a language model reads and predicts—usually a word piece, not always a whole word.",
  "problemStatement": "Models do not read raw characters or whole documents directly; they need a fixed vocabulary of units to embed and score for next-token prediction.",
  "coreIdea": "A tokenizer maps text to integer token IDs—the first step on the path from tokens to probabilities. Each ID is embedded into a vector, transformed through the stack, and scored so the model can rank candidates for the next token.",
  "sections": {
    "whatItIs": {
      "title": "What It Is",
      "body": "A token is one step in the model's discrete text vocabulary. Subword tokenizers often split rare words into pieces so the vocabulary stays finite while still covering open vocabulary text."
    },
    "whyItMatters": {
      "title": "Why It Matters",
      "body": "Tokenization sets context length in tokens, shapes cost per request, and determines whether two strings look identical to the model. Mismatched tokenizers between training and serving are a common integration bug."
    },
    "simpleExample": {
      "title": "Simple Example",
      "body": "The phrase \"language models\" might become two or three tokens depending on the tokenizer. Each ID is looked up in an embedding table before attention layers run."
    },
    "whereItAppears": {
      "title": "Where It Appears",
      "body": "Tokens appear at the input embedding layer, in autoregressive next-token scoring, and in KV-cache accounting during decoding. Attention modules operate on token positions, not raw characters. From here, follow the curated link to embeddings to continue the token-to-probability chain."
    },
    "commonConfusions": {
      "title": "Common Confusions",
      "body": "Tokens are not always words; byte-level BPE can emit single-character tokens. Token count also differs from word count and from Unicode grapheme count."
    },
    "related": {
      "title": "Related Concepts And Modules"
    },
    "tags": {
      "title": "Tags"
    },
    "references": {
      "title": "References"
    }
  },
  "callouts": {
    "readerShortcut": {
      "title": "Reader Shortcut",
      "body": "When someone says \"128k context,\" they mean 128k tokens—the same units the tokenizer emits before embeddings and next-token scoring. Use the related docs below to step from tokens into embeddings and onward toward logits and softmax."
    }
  },
  "assets": {
    "conceptMap": {
      "alt": "Diagram showing text flowing through a tokenizer into token IDs and embeddings",
      "caption": "How raw text becomes token IDs before the transformer stack"
    }
  },
  "graph": {
    "nodes": {
      "rawText": {
        "label": "Raw text"
      },
      "tokenizer": {
        "label": "Tokenizer"
      },
      "tokenIds": {
        "label": "Token IDs"
      },
      "embeddings": {
        "label": "Embeddings"
      }
    }
  }
}
