{
  "title": "Tokens Per Second",
  "description": "The serving metric that measures how quickly a model generates output tokens once streaming has begun.",
  "openingSummary": "Tokens per second counts generated output tokens over elapsed generation time. It describes streaming pace for one answer, not queue wait, time to first token, or fleet-wide throughput across many users.",
  "sections": {
    "whatItIs": {
      "title": "What It Is",
      "body": "Tokens per second is the rate at which a serving system emits newly generated output tokens during an answer. The count is output tokens divided by the elapsed time spent generating those tokens after streaming begins. A token is a model-specific text unit from the tokenizer, not necessarily one word, so the same sentence can yield different token counts on different models or tokenizers."
    },
    "whyItMatters": {
      "title": "Why It Matters",
      "body": "Readers feel tokens per second as how fast a streamed reply arrives once it starts. Operators track it separately from time to first token, inter-token latency, and fleet throughput because it answers a different question: how quickly output tokens appear during generation."
    },
    "simpleExample": {
      "title": "Simple Example",
      "body": "Imagine a chat answer that streams one hundred output tokens in five seconds after the first token appears. The tokens-per-second figure for that generation window is twenty output tokens per second. The timer starts when generation begins and stops when the answer finishes or the reader stops the stream."
    },
    "whatChangesTheNumber": {
      "title": "What Changes The Number",
      "body": "Prompt length changes how much prefill work runs before the generation window opens, so a long prompt can delay streaming even though prefill time is usually excluded from tokens per second. Output length changes how many decode steps the answer needs and how long the measured window lasts. Each decode step produces one more token, so a longer answer spends more time in the repeated generation loop. Stopping behavior also matters: an end-of-sequence token, a max-token limit, or a user cancel ends the timer at different points and can make two similar prompts report different rates."
    },
    "streamingVsThroughput": {
      "title": "Streaming Pace Versus Fleet Throughput",
      "body": "One reader's streaming pace is the tokens-per-second rate for a single answer during its generation window. Fleet throughput is the total output tokens a deployment serves across many requests and users over the same wall-clock period. A system can show high fleet throughput when it batches many requests together while each individual stream still feels slower. Comparing one user's chat speed to a provider's published throughput figure mixes two different measurements."
    },
    "commonConfusions": {
      "title": "Common Confusions",
      "body": "Tokens per second is not the same as time to first token, inter-token latency, or fleet throughput. Time to first token measures the wait before streaming starts. Inter-token latency measures the gap between individual tokens. Fleet throughput counts total output across many requests over time."
    },
    "servingPath": {
      "title": "Serving Path",
      "body": "Observed tokens per second depends on decode work, cache reuse, batching, and scheduling. Follow prefill for the prompt burst before output begins, decode for the repeated next-token loop, and the prefill/decode split for how serving systems stage the two phases."
    },
    "related": {
      "title": "Related Concepts And Modules"
    },
    "tags": {
      "title": "Tags"
    },
    "references": {
      "title": "References"
    }
  },
  "links": {
    "prefill": "Prefill",
    "decode": "Decode",
    "kvCache": "KV cache",
    "prefillDecodeSplit": "Prefill/decode split"
  }
}
