---
messageNamespace: "local"
assetNamespace: "local"
publishedAt: "2026-07-08"
updatedAt: "2026-07-08"
authors:
  - site-team
tags:
  - foundations
  - alignment
relatedDocIds:
  - training-regime.pretraining
status: "published"
---

import { Callout } from "@/features/docs/components/Callout";
import { T } from "@/features/docs/components/T";
import { TagPillList } from "@/features/docs/components/TagPillList";
import { TrainingSignalStackedChartFromDefault } from "@/features/graphs/training-signal/TrainingSignalStackedChartFromDefault";
import { TrainingSignalEvolutionBlogVisual } from "@/features/training-signal-evolution/TrainingSignalEvolutionBlogVisual";

# <T k="title" />

<T k="contextSentence" />

<TagPillList tags={["foundations", "alignment"]} />

## Summary

<T k="takeaway" />

## Why the training story changed

Early public explanations of large language models often stopped at one idea: scrape a broad pretraining corpus from the open web and predict the next token. That corpus still matters, but the models readers use today are shaped by additional supervision and feedback that arrives after broad corpus learning.

Instruction examples teach format-following behavior. Preference comparisons steer style and safety. Verifiable reinforcement-learning tasks reward outcomes that can be checked automatically. On-policy distillation and self-distillation recycle a model's own outputs back into training. The timeline below walks through each stage and explains what changed in the training signal readers actually observe.

## How training signals evolved

Web-scale pretraining remains the foundation. Later stages add mid-training and post-training supervision, prompting-time adaptation, and on-policy feedback loops that now share more of the job of shaping deployed behavior.

### Training-signal mix over time

The stacked chart below is a **conceptual illustration**. Band heights show illustrative relative emphasis across three eras, not measured dataset percentages or published benchmarks.

<TrainingSignalStackedChartFromDefault caption="Illustrative training-signal mix across three eras. Values are conceptual, not sourced measurements." />

<Callout type="note" titleKey="callouts.conceptualChart.title">
  <T k="callouts.conceptualChart.body" />
</Callout>

The timeline below walks through the same shift in chronological order, naming each stage and explaining what changed in the training signal readers observe.

<TrainingSignalEvolutionBlogVisual />
