## Codebase Patterns
- Docs sidebar folder generation iterates `listDocsCollectionDefinitions()` from `src/lib/docs/docs-collection-definitions.ts`; assign published pages to folders by matching the first `docsSlug` segment to each definition's `routeSlug`.
- Grouped sidebar collections delegate to `buildGroupedSidebarNodes()` in `src/lib/navigation/docs-sidebar-grouping-adapter.ts`, which owns AI registry lookup and sidebar grouping resolver dispatch; the generic builder in `generated-docs-page-tree.ts` only handles ungrouped title sorting.
- When asserting grouped sidebar pages for the final separator in a section, filter nodes by the expected group's page URLs because trailing pages without a resolved group follow grouped content without their own separator.
- New registry records under `src/content/registry/` are picked up by `bun run prepare:content-runtime`, which regenerates `src/lib/content/generated/registry-runtime.generated.ts` before typecheck, lint, and tests run.
- Model registry records use typed relationship fields (`architectureIds`, `moduleIds`, `trainingRegimeIds`, `relatedIds`) plus backing `citation`, `paper`, and `organization` records; update reciprocal `usedByModelIds` / `relatedIds` on linked training regimes when adding model consumers.
- For post-training discovery when no generic `training-regime.post-training` record exists, link models to `concept.alignment` in `relatedIds` and keep `training-regime.pretraining` in `trainingRegimeIds`.
- Focused registry contract tests live beside other content tests in `src/lib/content/*-registry.test.ts` and should assert schema validity plus `getModelById` / `getPaperById` runtime resolution.
- New model pages under `src/content/docs/models/<slug>/` auto-register via `prepare:content-runtime`; use empty `{}` in `assets.json` until a graph asset is added in a later story.
- Model architecture graphs pair `src/content/registry/graphs/<slug>-architecture.json` with `assets.json` `architectureGraph.graphId` and localized `graph.nodes` / `assets.architectureGraph.alt` keys in `messages/en.json`; follow the DeepSeek-V4-Pro RoPE + RMSNorm graph pattern for modern decoder stacks.

## 2026-07-01T00:00:00Z - llama-3-model-page-001
- Established `model.llama-3` with Llama-family metadata, open-weights source type, 405B / 128K fields, and relationships to transformer architecture, autoregressive generation, attention, RoPE, context-window, pretraining, and alignment (post-training) records.
- Added backing `citation.llama-3-herd-of-models`, `citation.llama-3-meta-release`, `paper.llama-3`, and `organization.meta` records; linked `training-regime.pretraining` to the new model.
- Added `src/lib/content/llama-3-model-registry.test.ts` and updated pretraining contract tests for the new reciprocal model link.
- **Learnings for future iterations:**
  - `listRelatedRegistryRecords()` takes no arguments; use `deriveCuratedRelatedItems(source, listRelatedRegistryRecords(), PUBLISHED_DOCS_REGISTRY_IDS)` only after the model page is published.
  - Biome formatting may reflow multi-line JSON `tags` and `aliases` arrays on new registry files.
  - Llama 3 context length is stored as `131072` tokens (128K) in registry numeric fields.
---

## 2026-07-01T12:00:00Z - llama-3-model-page-002
- Published canonical English Llama 3 model page at `/docs/models/llama-3` with `page.mdx`, `messages/en.json`, and empty `assets.json` (architecture graph deferred to story 003).
- Added focused page contract test in `src/lib/content/llama-3-model-page.test.tsx` covering route registration, section rendering, explainer-not-benchmark copy, and registry-backed metadata.
- Verified production build and browser-visible rendering on port 3456.
- **Learnings for future iterations:**
  - Prose auto-linking splits phrases like `128K tokens`, so page tests should assert on separate substrings or message keys rather than contiguous linked text.
  - Story 002 can ship without `ModelArchitectureGraph`; omit the component until graph registry records and `assets.json` graph config exist.
  - `loadModelPage("llama-3")` plus `ModulePageProviders` is the standard focused render test pattern for model pages.
---

## 2026-07-01T13:10:00Z - llama-3-model-page-003
- Expanded architecture, modules, training, and practical-notes copy to connect decoder-only transformers, attention, RoPE, autoregressive generation, context window, pretraining, and post-training concepts.
- Added `graph.llama-3-architecture` registry graph with GQA, RoPE, RMSNorm, and SwiGLU nodes; wired `ModelArchitectureGraph` through `assets.json` and localized graph labels/alt text.
- Extended `llama-3-model-page.test.tsx` to assert registry-backed module/training links and architecture graph rendering.
- Verified production build and browser-visible rendering on port 3456.
- **Learnings for future iterations:**
  - Training regime links render at `/docs/training/pretraining`, not `/docs/training-regimes/pretraining`.
  - Prose auto-linking splits phrases like `Rotary position embeddings`; assert on substrings or href targets instead of contiguous text.
  - `ModelTrainingSummary` lists `trainingRegimeIds` and `paperIds` only; post-training discovery for models without a generic post-training regime still flows through `relatedIds` (e.g. `concept.alignment`) in derived related docs.
---

## 2026-07-01T14:30:00Z - generic-sidebar-collection-builder-001
- Refactored `buildGeneratedDocsPageTree` to iterate `listDocsCollectionDefinitions()` instead of hardcoded `SECTION_ORDER`, assigning published pages to folders by each definition's `routeSlug`.
- Preserved existing per-collection node generation (grouping logic unchanged for this story) and English sidebar folder titles via `SIDEBAR_FOLDER_TITLES` keyed by `DocsCollectionId`.
- Added focused tests asserting collection-driven folder order and one-to-one page assignment across all configured collections.
- **Learnings for future iterations:**
  - Story 001 only drives folder iteration and page bucketing; AI registry imports and grouping resolvers remain in the generic builder until story 002 introduces the adapter.
  - `collectSidebarPageLinks` from `docs-sidebar-contract.ts` is the preferred helper for asserting sidebar page coverage without scraping HTML.
  - Folder display names are still a local English map; browse `messageKeys.browse.sectionTitle` could replace it later if sidebar localization is needed.
---

## 2026-07-01T15:40:00Z - generic-sidebar-collection-builder-002
- Added `docs-sidebar-grouping-adapter.ts` with `buildGroupedSidebarNodes()` owning AI registry lookup (`getConceptById`, `getModuleById`, `getTrainingRegimeById`, `getSystemById`) and sidebar grouping resolver dispatch for glossary, concepts, modules, training, and systems.
- Refactored `generated-docs-page-tree.ts` to route grouped collections through the adapter via each definition's `sidebarGroupingResolverId`; ungrouped collections (models, papers) remain title-sorted in the generic builder.
- Added adapter tests for configured resolver ids and explicit rejection of unsupported resolver ids.
- **Learnings for future iterations:**
  - Keep generic node construction (`createPageNode`, `sortPages`) in the generic builder; move registry-backed grouping and separator assembly into the adapter.
  - `assertSupportedSidebarGroupingResolverId()` provides a runtime guard when resolver ids cross string boundaries; typed `DocsCollectionSidebarGroupingResolverId` covers the happy path at compile time.
  - Existing `generated-docs-page-tree.test.ts` behavioral assertions remain valid without changes because sidebar output is unchanged.
---

## 2026-07-01T16:55:00Z - generic-sidebar-collection-builder-003
- Added focused page-tree tests asserting models and papers (collections without `sidebarGroupingResolverId`) render title-sorted page nodes with no separator entries.
- Added `GPT_3_MODEL_URL` and `DEEPSEEK_V4_PAPER_URL` constants to `docs-sidebar-contract.ts` and asserted representative ungrouped link names/URLs through `collectSidebarPageLinks`.
- Verified existing `generateUngroupedNodes()` behavior already satisfies story acceptance criteria; no production code changes required.
- **Learnings for future iterations:**
  - Use `getPageNodeEntries()` plus `sortPagesByTitle()` against `loadPublishedDocsPagesSync("en")` to assert ungrouped sidebar ordering without hardcoding every page URL.
  - Ungrouped collection verification belongs in `generated-docs-page-tree.test.ts`; `source.test.ts` still checks URL presence but not title order or separator absence.
---

## 2026-07-01T17:35:00Z - generic-sidebar-collection-builder-004
- Added page-tree tests asserting grouped separator labels and page contiguity (existing), per-group title sorting by localized page name, and trailing placement of pages without a resolved group after all grouped content.
- Verified grouped collection behavior already satisfied story acceptance criteria through the adapter's `groupPages()` implementation; no production code changes required.
- Files changed: `src/lib/navigation/generated-docs-page-tree.test.ts`
- **Learnings for future iterations:**
  - Concepts, modules, and systems currently have published pages without a resolved sidebar group; they render in a title-sorted tail after the last grouped separator.
  - For the final group in a grouped folder, slice assertions must filter by `group.pageUrls` or stop before trailing ungrouped pages because `findNextSeparatorIndex` returns `-1`.
  - Reuse `collectUngroupedPages()` with each `GROUPED_SECTIONS` resolver to assert tail placement without hardcoding every fallback page slug.
---
