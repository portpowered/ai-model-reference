## Codebase Patterns
- Model pages live under `src/content/docs/models/{slug}/` with `page.mdx`, `messages/en.json`, and `assets.json`; follow `gpt-3` as the closest GPT-family decoder-only reference.
- Model registry records live at `src/content/registry/models/{slug}.json` with `id: model.{slug}`, family `gpt`, and `citationIds` pointing to existing citation records.
- Reuse existing `paper.gpt-2-report` and `citation.gpt-2-report` for GPT-2 source attribution; do not create duplicate paper/citation records.
- Run `bun run typecheck` for typecheck gate; `make validate-data` for registry/content validation after adding records.
- Page-local scope: avoid shared helper edits, generated artifact commits, and page-specific tests unless special behavior requires them.
- Model `sourceType` enum is `open-weights` | `closed` | `research` (not `public`); use `open-weights` for GPT-2 since smaller checkpoints were publicly released.
- Registry-only slices need no page bundle: `make validate-data` regenerates ignored runtime artifacts locally but they stay untracked.
- Model page `assets.json` graph references require a matching `src/content/registry/graphs/{slug}-architecture.json` record or `validate-registry` fails; include the graph registry when adding `ModelArchitectureGraph` even if a later story refines the visual.
- Use `module.standard-ffn` (not `module.feed-forward-network`) in GPT-2 graph nodes to match the model registry `moduleIds`.
- GPT-family decoder architecture graphs can add a page-local `next-token-selection` root node linked to `concept.sampling-overview` when the story needs explicit sampling/selection teaching beyond baseline `output-probabilities`.

## 2026-07-08T13:10:00Z - gpt-2-model-page-current-main-001
- Verified GPT-2 absence and existing record reuse before any slice work.
- **Verification results:**
  - `src/content/docs/models/gpt-2/` — absent (safe to create in story 003)
  - `src/content/registry/models/gpt-2.json` — absent (safe to create in story 002)
  - `src/content/registry/papers/gpt-2-report.json` — exists (`paper.gpt-2-report`)
  - `src/content/registry/citations/gpt-2-report.json` — exists (`citation.gpt-2-report`)
  - No duplicate GPT-2 paper or citation records will be introduced; reuse the existing pair.
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - Use `gpt-3` model page and `model.gpt-3.json` registry as the primary template for GPT-2 (decoder-only, GPT family, pretraining, byte-level tokenization).
  - GPT-2 paper record has empty `modelIds`; story 002 should link `model.gpt-2` via registry `citationIds` and may update paper `modelIds` only if validation requires it—stay page-local.
  - `module.byte-level-tokenization` is the GPT-2 tokenization module already linked from the paper record.
---

## 2026-07-08T13:30:00Z - gpt-2-model-page-current-main-002
- Added `src/content/registry/models/gpt-2.json` as the canonical published model registry record for GPT-2.
- Record includes slug `gpt-2`, aliases (GPT-2, Generative Pre-trained Transformer 2), GPT family, text modality, `open-weights` source type, release date 2019-02-14, parameter/context metadata, and `citation.gpt-2-report`.
- Linked architecture (`concept.transformer-architecture`, `concept.autoregressive-generation`), modules (byte-level tokenization, multi-head attention, standard FFN, layer norm, learned positional embeddings), training regime (pretraining), and related concepts (self-attention, positional encodings, tokenizers, logit, softmax, sampling-overview).
- Files changed: `src/content/registry/models/gpt-2.json`
- **Learnings for future iterations:**
  - `gpt-3.json` is the best structural template for GPT-family decoder-only model records; adapt parameter counts and `sourceType` per model.
  - `relatedIds` is the right place for logits/softmax/sampling concept links; existing model records rarely put those in `architectureIds`/`moduleIds`.
  - No paper `modelIds` or pretraining `usedByModelIds` back-link updates were required for validation to pass.
  - Full `bun run test` suite takes ~34 minutes locally; `make validate-data` + `bun run typecheck` are faster gates for registry-only changes.
---

## 2026-07-08T13:55:00Z - gpt-2-model-page-current-main-003
- Added canonical GPT-2 model page bundle under `src/content/docs/models/gpt-2/` with production model template structure, localized English messages, and assets referencing `graph.gpt-2-architecture`.
- Page explains GPT-2 as a decoder-only Transformer with next-token pretraining, byte-level tokenization, logits/softmax/sampling generation flow, historical significance, and practical context for modern readers.
- Added `src/content/registry/graphs/gpt-2-architecture.json` (GPT-3-style decoder stack adapted for GPT-2 module IDs) so asset validation and `ModelArchitectureGraph` rendering succeed.
- Files changed: `src/content/docs/models/gpt-2/page.mdx`, `src/content/docs/models/gpt-2/messages/en.json`, `src/content/docs/models/gpt-2/assets.json`, `src/content/registry/graphs/gpt-2-architecture.json`
- **Learnings for future iterations:**
  - SSR render via `loadModelPage` + `ModulePageProviders` is a fast browser-verification substitute when full `bun run build` is slow locally.
  - Story 004 may still refine the architecture graph (e.g., explicit next-token sampling node) even though a baseline graph was required for story 003 validation.
  - `relatedDocs` message keys for transformer architecture, byte-level tokenization, and sampling-overview give curated discovery reasons without a second `RelatedDocs` block.
---

## 2026-07-08T14:25:00Z - gpt-2-model-page-current-main-004
- Refined `graph.gpt-2-architecture` to teach the full decoder-only next-token flow: added `next-token-selection` root node (`concept.sampling-overview`), renamed the linear head label to Logits with `concept.logit`, shifted stack layout to preserve vertical flow, and wired a new edge from output probabilities to next-token selection.
- Updated localized graph node messages and architecture-graph alt text in `messages/en.json` to describe logits, softmax probabilities, and next-token sampling/selection.
- Verified via `make validate-data`, `bun run typecheck`, `buildRegistryFlowGraph` for 16 nodes/13 edges, SSR render of the GPT-2 page, and graph-flow/a11y tests.
- Files changed: `src/content/registry/graphs/gpt-2-architecture.json`, `src/content/docs/models/gpt-2/messages/en.json`
- **Learnings for future iterations:**
  - Story 003's baseline GPT-3-style graph satisfies validation but story 004 should add explicit sampling and Logits labeling when acceptance criteria call out the generation chain.
  - Shifting all downstream node Y positions by ~80px is the smallest layout change when inserting a new root output node above `output-probabilities`.
  - `bun run test:full-fast` can surface unrelated flaky `source` initialization and timeout failures; graph-specific tests (`graph-flow.test.ts`, `registry-graph-flow.a11y.test.tsx`) are reliable gates for architecture graph edits.
---
