## Codebase Patterns
- Training-regime registry records live in `src/content/registry/training-regimes/{slug}.json` and auto-load via `loadRegistry`.
- Published training-regime records require `releaseDate`, `authors`, `sourceId`, and at least one `citationIds` entry.
- Reuse existing citation records (e.g. `citation.training-language-models-to-follow-instructions-with-human-feedback`) instead of duplicating sources.
- `regimeType: "post-training"` with `sidebarGrouping.training: "post-training"` groups post-pretraining regimes like specialist-training.
- Valid training-regime tags include `alignment` and `foundations` for post-training adaptation regimes.

## 2026-07-01T00:00:00Z - supervised-fine-tuning-training-regime-page-001
- Created published `training-regime.supervised-fine-tuning` registry record with SFT aliases, post-training metadata, links to pretraining/DPO/alignment, and reused InstructGPT citation.
- Files changed: `src/content/registry/training-regimes/supervised-fine-tuning.json`
- **Learnings for future iterations:**
  - Published training-regime records need `releaseDate`, `authors`, and `sourceId` in addition to `citationIds`.
  - No instruction-tuning registry record exists yet; link to pretraining, DPO, and `concept.alignment` for nearby discovery.
  - `variantGroup: "language-model-training-stages"` matches pretraining for lifecycle-stage grouping.
---
