## Codebase Patterns
- Reuse `isTerminalCompleteState` and `parseTerminalCompleteWorkItems` from `planner-merged-lane-evidence.ts` when discovering terminal queue lanes; extend with TERMINAL-type near-terminal parsing in the landing audit module instead of duplicating complete-state heuristics.
- Terminal lane landing audit discovery accepts `--work-list-json`, `--worktrees-dir`, explicit lane names, and `landingCandidates` fixture injection following other planner report modules.
- Missing optional lane metadata should surface as `unknown` or `unavailable` evidence statuses rather than throwing during discovery.
- Export `isTerminalCompleteState` when other factory modules need the same terminal-complete queue heuristic.

## 2026-07-01T00:00:00.000Z - terminal-lane-main-branch-landing-audit-001
- Implemented read-only terminal/near-terminal lane candidate discovery in `terminal-lane-main-branch-landing-audit.ts` with queue TERMINAL evidence, explicit lane input, and worktree metadata enrichment.
- Added focused tests for terminal-complete vs near-terminal queue lanes, explicit lane unknown metadata, fixture injection, and human-readable discovery formatting.
- Exported `isTerminalCompleteState` from `planner-merged-lane-evidence.ts` for shared terminal-state detection.
- Updated `factory-linkage-relevant-files.md` with the new audit module entry.
- Files changed: `src/lib/factory/terminal-lane-main-branch-landing-audit.ts`, `src/lib/factory/terminal-lane-main-branch-landing-audit.test.ts`, `src/lib/factory/planner-merged-lane-evidence.ts`, `docs/internal/processes/factory-linkage-relevant-files.md`
- **Learnings for future iterations:**
  - Patterns discovered: candidate discovery follows the `collect*Snapshot` / `discover*` split used by batch collision preflight and merged lane evidence; worktree branch identity comes from `discoverWorktreeLaneRecords` with metadata fallback via `readWorktreeLaneMetadata`.
  - Gotchas encountered: stamped lane metadata normalizes `branchMetadataSource` to `metadata` even when authored as `prd` in fixtures; create `.claude/` parent dirs before writing lane metadata in tests.
  - Useful context: story 002 should build main-branch surface comparison on top of `TerminalLaneLandingCandidate` without re-parsing queue/worktree evidence.
---
