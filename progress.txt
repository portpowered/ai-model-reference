## Codebase Patterns
- Use `bun run report:merged-pr-drain-rows-reconciliation` to capture read-only queue/PR/worktree evidence for merged PR drain rows #281/#282/#284/#286; target session is `930b51a6-07ce-44e6-a639-7a6217f6e864`.
- When running planner reports from a nested git worktree, resolve main-repo paths via `resolveMainRepoRoot` / `resolveDefaultWorktreesDir` (`git rev-parse --git-common-dir`) so `.claude/worktrees` and root checkout evidence point at `/Users/abdifamily/work/learn-agent-factories`, not the nested worktree checkout.
- Distinguish merged PR truth from queue completion truth explicitly; all four PRs can be MERGED while drain rows remain `init/INITIAL`.
- Use `classifyMergedPrDrainRowOutcome` / `buildMergedPrDrainRowsClassificationReport` for per-row `consume` / `complete` / `no-op` outcomes; stale `init/INITIAL` drain rows with terminal-complete content lanes classify as `consume`, and rows without drain rows classify as `no-op` (`already-settled`).
- Use `buildMergedPrDrainRowNoOpHandoff` / `buildMergedPrDrainRowsNoOpReport` for explicit no-op handoffs with `row-left-untouched=true`, `missing-evidence`, and `next-safe-owner-action` when blockers apply.
- Use `buildMergedPrDrainRowsFinalVerificationReport` for post-reconciliation root checkout, baseline-aware content safety (`observedDirtyPaths` on root/worktree evidence), and queue transition summaries; compare dirty paths with `findNewDirtyPaths` so pre-existing worktree drift does not fail verification.
- Accepted consume operation for standalone merged drain idea rows is `manual-drain-row-move-to-complete` via `you work move <drain-work-id> complete --session 930b51a6-07ce-44e6-a639-7a6217f6e864`; the standard factory `consume` workstation does not apply without a paired task token.
- Accepted complete operation for non-terminal drain rows is `manual-drain-row-terminal-completion` via the same `you work move <drain-work-id> complete --session <session>` command; `buildMergedPrDrainRowCompleteHandoff` reclassifies to no-op when unfinished implementation/review, PR mismatch, or missing metadata block the transition.

## 2026-07-02T19:18:00Z - merged-pr-drain-rows-281-282-284-286-reconciliation-001
- Implemented read-only merged PR drain row evidence capture module, planner report script, unit tests, and process documentation for PRs #281, #282, #284, and #286.
- Files changed: `src/lib/factory/merged-pr-drain-rows-reconciliation.ts`, `src/lib/factory/merged-pr-drain-rows-reconciliation.test.ts`, `scripts/report-merged-pr-drain-rows-reconciliation.ts`, `package.json`, `docs/internal/processes/merged-pr-drain-rows-reconciliation-relevant-files.md`, `docs/internal/processes/factory-linkage-relevant-files.md`
- **Learnings for future iterations:**
  - Model-atlas content lane queue lives in session `930b51a6-07ce-44e6-a639-7a6217f6e864`; default `you work list` omits these rows.
  - Drain rows `ltx-23-pr281-drain`, `mamba-pr282-drain`, and `glossary-decomposition-pr284-conflict-refresh` are `init/INITIAL` while content lanes are `complete/terminal`; `bpe-page` has no dedicated drain row.
  - All four merge commits are in `origin/main` lineage at SHA `209d1bd8ced0cced5fd99992fe50f23296d126e8`.
  - Use `gh pr view <number>` (not open-only `gh pr list`) for merged PR truth lookup.
---

## 2026-07-02T19:22:00Z - merged-pr-drain-rows-281-282-284-286-reconciliation-002
- Implemented per-row `consume` / `complete` / `no-op` classification on top of the evidence report, extended the planner CLI to emit classification in text/JSON output, and documented live outcomes for all four rows.
- Files changed: `src/lib/factory/merged-pr-drain-rows-reconciliation.ts`, `src/lib/factory/merged-pr-drain-rows-reconciliation.test.ts`, `scripts/report-merged-pr-drain-rows-reconciliation.ts`, `docs/internal/processes/merged-pr-drain-rows-reconciliation-relevant-files.md`
- **Learnings for future iterations:**
  - Live classification (2026-07-02T19:21:11Z UTC): `ltx-23`/#281, `MAMBA`/#282, and `glossary-decomposition`/#284 → `consume`; `bpe-page`/#286 → `no-op` (`already-settled`, no drain row).
  - Unavailable worktree metadata does not block classification when queue tokens and PR truth are sufficient; only stamped PR mismatch blocks via `row-pr-mismatch`.
  - `complete` applies when content lane is terminal-complete but the drain row is non-terminal (not `init/INITIAL`).
---

## 2026-07-02T19:25:00Z - merged-pr-drain-rows-281-282-284-286-reconciliation-003
- Implemented consume handoff builders/executors for merged drain rows, extended the planner CLI with `--execute-consume`, executed live consume moves for all three drain rows, and documented post-action queue state.
- Files changed: `src/lib/factory/merged-pr-drain-rows-reconciliation.ts`, `src/lib/factory/merged-pr-drain-rows-reconciliation.test.ts`, `scripts/report-merged-pr-drain-rows-reconciliation.ts`, `docs/internal/processes/merged-pr-drain-rows-reconciliation-relevant-files.md`
- **Learnings for future iterations:**
  - Standalone drain idea rows at `init/INITIAL` with merged PRs and terminal content lanes consume via `you work move <work-id> complete --session <session>`, not the paired `idea:to-complete` + `task:to-complete` factory consume workstation.
  - Live consume execution (2026-07-02T19:24Z UTC): `ltx-23-pr281-drain`, `mamba-pr282-drain`, and `glossary-decomposition-pr284-conflict-refresh` all moved from `init/INITIAL` to `complete/TERMINAL`.
  - After consume, classification correctly reclassifies those rows as `no-op` (`already-terminal`) with empty consume handoff sections.
---

## 2026-07-02T19:32:00Z - merged-pr-drain-rows-281-282-284-286-reconciliation-004
- Implemented complete handoff builders/executors for non-terminal drain rows (`manual-drain-row-terminal-completion`), blocker reclassification to no-op, unified reconciliation output, CLI `--execute-complete`, and live verification showing zero rows need completion.
- Files changed: `src/lib/factory/merged-pr-drain-rows-reconciliation.ts`, `src/lib/factory/merged-pr-drain-rows-reconciliation.test.ts`, `scripts/report-merged-pr-drain-rows-reconciliation.ts`, `docs/internal/processes/merged-pr-drain-rows-reconciliation-relevant-files.md`
- **Learnings for future iterations:**
  - `complete` differs from `consume`: it applies when drain rows are non-terminal but not `init/INITIAL`; both use `you work move <work-id> complete --session <session>`.
  - Complete handoff building is stricter than classification — missing worktree metadata reclassifies to no-op even when queue/PR evidence suffices for classification.
  - Live state (2026-07-02T19:31Z UTC): all four rows classify as `no-op`; three drain rows are `already-terminal` after story 003 consume; zero rows classified as `complete`.
---

## 2026-07-02T19:36:00Z - merged-pr-drain-rows-281-282-284-286-reconciliation-005
- Implemented no-op handoff builders/reports with explicit `row-left-untouched`, `missing-evidence`, and `next-safe-owner-action` fields; extended unified reconciliation output and CLI text/JSON to include no-op handoff section.
- Files changed: `src/lib/factory/merged-pr-drain-rows-reconciliation.ts`, `src/lib/factory/merged-pr-drain-rows-reconciliation.test.ts`, `scripts/report-merged-pr-drain-rows-reconciliation.ts`, `docs/internal/processes/merged-pr-drain-rows-reconciliation-relevant-files.md`
- **Learnings for future iterations:**
  - Live no-op state (2026-07-02T19:35Z UTC): all four rows are `no-op` — three `already-terminal` post-consume drain rows plus `bpe-page` `already-settled`.
  - No-op handoffs are read-only; they never mutate queue rows and surface owner actions only for unfinished implementation/review or PR/queue blockers.
  - `buildMergedPrDrainRowsReconciliationOutput` now always includes `noOpReport` alongside consume and complete reports.
---

## 2026-07-02T19:45:00Z - merged-pr-drain-rows-281-282-284-286-reconciliation-006
- Implemented final verification report with post-reconciliation root checkout evidence, baseline-aware content safety (`observedDirtyPaths` on root/worktree evidence), queue transition summaries, unified CLI/JSON output, and process documentation.
- Files changed: `src/lib/factory/merged-pr-drain-rows-reconciliation.ts`, `src/lib/factory/merged-pr-drain-rows-reconciliation.test.ts`, `scripts/report-merged-pr-drain-rows-reconciliation.ts`, `docs/internal/processes/merged-pr-drain-rows-reconciliation-relevant-files.md`
- **Learnings for future iterations:**
  - Do not `.trim()` git porcelain lines before `slice(3)` — trimming removes the leading status-column space and corrupts path extraction.
  - Compare dirty-path baselines with `findNewDirtyPaths` so pre-existing worktree drift does not fail final verification; capture `observedDirtyPaths` during evidence gathering.
  - Live final state (2026-07-02T19:45Z UTC): all four rows `left-untouched`; queue transitions occurred in story 003; read-only verification passes with content safety intact.
---
