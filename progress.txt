## Codebase Patterns
- Conflict-refresh evidence for PR drain lanes belongs in `docs/internal/processes/<work-item>-relevant-files.md`; link from `factory-linkage-relevant-files.md`.
- Model-atlas page lanes live on factory session `930b51a6-07ce-44e6-a639-7a6217f6e864`; default session and documented `0fdc5077-95ed-4396-a183-06e5b16555ca` omit or 404 for byte-level-tokenization-page queue tokens.
- Use `you work list --session 930b51a6-07ce-44e6-a639-7a6217f6e864 --name <page-lane> --json` for live queue state on tokenizer/content page lanes.
- PR #289 mergeability was DIRTY in the planner snapshot but is CLEAN/MERGEABLE after commit `85dfc333` merged `origin/main`; always re-fetch live `gh pr view` before classifying conflict-refresh outcomes.
- Collision preflight for byte-level-tokenization-page reports `active-lane-overlap=none` even when nearby tokenizer worktrees exist; check stamped PR linkage separately (`byte-level-tokenization-page` metadata still has `pullRequest: null`).

- PR #289 conflict-refresh drain outcomes belong in a `## Drain outcome` section of the lane evidence doc; classify exactly one of `refresh-safe`, `handoff-required`, or `stale-or-duplicate` with explicit rejection rationale for the other two.
- Story 003 refresh work runs in `.claude/worktrees/byte-level-tokenization-page` (not the conflict-refresh worktree); run `bun install` there before local validation when `node_modules` is absent.

## 2026-07-02T17:25:00Z - byte-level-tokenization-pr289-conflict-refresh-001
- Captured fresh queue, lane metadata, branch drift, origin/main, GitHub PR #289, and tokenizer-lane collision evidence in `docs/internal/processes/byte-level-tokenization-pr289-conflict-refresh-relevant-files.md`.
- Linked the evidence snapshot from `factory-linkage-relevant-files.md`.
- Files changed: `docs/internal/processes/byte-level-tokenization-pr289-conflict-refresh-relevant-files.md`, `docs/internal/processes/factory-linkage-relevant-files.md`
- **Learnings for future iterations:**
  - Queue on session `930b51a6-07ce-44e6-a639-7a6217f6e864` still shows `idea:to-complete` + `task:init` while the page PRD and PR #289 head already complete all page stories.
  - PR #289 is MERGEABLE/CLEAN with all checks SUCCESS; prior DIRTY state was cleared by merge commit `85dfc333`.
  - `origin/main` already publishes the byte-level tokenization page bundle; PR #289 carries incremental message/test deltas on top.
  - Lane metadata for `byte-level-tokenization-page` lacks stamped PR #289 linkage despite an open GitHub PR.
  - `bun run typecheck` passes as the story quality gate.
---

## 2026-07-02T17:32:00Z - byte-level-tokenization-pr289-conflict-refresh-002
- Classified PR #289 drain outcome as `refresh-safe` with evidence tables and explicit rejection of `handoff-required` and `stale-or-duplicate` in `docs/internal/processes/byte-level-tokenization-pr289-conflict-refresh-relevant-files.md`.
- Updated factory linkage index blurb to reference the selected outcome.
- Files changed: `docs/internal/processes/byte-level-tokenization-pr289-conflict-refresh-relevant-files.md`, `docs/internal/processes/factory-linkage-relevant-files.md`
- **Learnings for future iterations:**
  - When PR #289 is already CLEAN/MERGEABLE after lane merge `85dfc333`, story 002 still needs the formal single-outcome report before story 003 verification work.
  - Reject `stale-or-duplicate` when PR diff vs `origin/main` still carries focused tests or message deltas absent on main (e.g. `byte-level-tokenization-page-contract.test.ts`).
  - `bun run typecheck` passes as the story quality gate.
---

## 2026-07-02T18:05:00Z - byte-level-tokenization-pr289-conflict-refresh-003
- Executed `refresh-safe` outcome: merged `origin/main` (`d9ef966b`) into `byte-level-tokenization-page` at commit `f010c064` with no conflicts; pushed to `origin/byte-level-tokenization-page`.
- Ran page-level validation in the page lane worktree: typecheck, lint, targeted byte-level tokenization tests (22 pass), and `validate-data`.
- Documented refresh completion and post-merge drift in `docs/internal/processes/byte-level-tokenization-pr289-conflict-refresh-relevant-files.md`.
- Files changed: `docs/internal/processes/byte-level-tokenization-pr289-conflict-refresh-relevant-files.md`
- Page lane commit (pushed separately on `byte-level-tokenization-page`): `f010c064` merge of `origin/main`
- **Learnings for future iterations:**
  - Prior merge `85dfc333` left the branch behind main by 7 commits; a clean ort merge at `f010c064` brought in only `ltx-23` model surfaces with no byte-level conflicts.
  - Conflict-refresh lane docs live on `byte-level-tokenization-pr289-conflict-refresh`; git mutations for PR #289 belong in the `byte-level-tokenization-page` worktree.
  - `bun install` is required in page worktrees before running typecheck/tests when `node_modules` is missing.
---
