# high level data modell

There exists models

models have modules
models are tagged
models have training regimes
models have a size

modules are of a type of variation
modules are tagged

training regimes are tagged


## page
a page describes something ( a model, a module, a concept)